import React from "react";
import { data } from "../restApi.json";
const Team = () => {
  return (
    <section className="team" id="team">
      <div className="container">
        <div className="heading_section">
          <h1 className="heading">OUR TEAM</h1>
          <p>
            Behind <strong>SÁNARA</strong> is a collective of chefs, creators,
            and visionaries united by one purpose: to craft experiences that
            linger far beyond the plate. Our team blends cultural roots,
            culinary mastery, and a shared reverence for ritual — transforming
            ingredients into emotion, and meals into memories.
          </p>
        </div>
        <div className="team_container">
          {data[0].team.map((element) => {
            return (
              <div className="card" key={element.id}>
                <img src={element.image} alt={element.name} />
                <h3>{element.name}</h3>
                <p>{element.designation}</p>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
};

export default Team;
